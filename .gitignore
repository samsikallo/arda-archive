# The public archive. This repository is served at samsikallo.github.io/arda-archive,
# so anything committed here is published, and published is not undone by deleting.
#
# WHY THIS FILE EXISTS AT ALL, AND WHY ITS ABSENCE WAS THE WORST OF THE FOUR DEPLOY
# FAULTS. The workshop above has carried a .gitignore for a long time, and its first
# rule is the books: "the books themselves are not ours to redistribute". This
# repository -- the PUBLIC one, the only one that actually publishes -- had no
# .gitignore of any kind. The protection was strongest where the consequence was
# smallest and absent where it was largest.
#
# The workshop's own comment records that three books were once tracked by accident,
# "because one is an .epub and one is a PDF saved with no extension at all", and they
# had to be cut out of history with git filter-branch TWICE, the first pass missing the
# paths they had lived at earlier. That is the cost being guarded against, and it is
# strictly worse here: a private repository can be rewritten quietly, a published one
# has already been fetched.
#
# The pre-commit hook refuses sources/ unconditionally and does not rely on this file.
# Both exist on purpose: the hook stops a deliberate `git add -f`, this stops an
# absent-minded `git add -A`, and neither covers the other's case.

# ---- books, in every form the shelf has ever held one -------------------------------
*.pdf
*.epub
*.mobi
*.azw3
*.djvu
sources/**
"secondary sources"/
secondary sources/

# ---- derived full text: rebuildable, large, and just as much theirs as the books ----
corpus/
*.folded

# ---- what the guards leave behind ---------------------------------------------------
# The deep-link, realms and portrait guards drive a real browser against probe pages
# written beside the real ones. They are removed on a clean run and survive a crash or
# an interrupted suite, which is exactly when someone is most likely to stage broadly.
_*.html
# ONE EXCEPTION, AND IT IS A RULING -- C632, 29 August 2026.
# C628 kept the four rejected typefaces "as the evaluation record, the specimen page
# included". The fonts are tracked; this page was caught by the rule above and had NO
# history in this repository at all -- so the record his ruling named was the one part of
# it with no copy that survives a clone or a restore. The Auditor established it
# independently (ls-files pathspec failure; check-ignore .gitignore:40; log --all empty).
# An ignored file is not a record, so this negation exists to make the ruling true.
!_typeface_specimen.html

# ---- ordinary noise ------------------------------------------------------------------
*.pyc
__pycache__/
.DS_Store
*.swp
*~
.tree-lease

# THE OWNER RULED ON 29 JULY that the archive does not republish Tolkien's or
# Christopher Tolkien's artwork. arda_traced.svg is 215,090 vector paths traced from his
# published plates -- his linework, not ours -- and it was reachable from map.html behind a
# button reading "Christopher's hand". It stays in the workshop as a research artefact,
# because the archive still measures its own map against his geometry; it is not published.
arda_traced.svg

# arda_his_anchors.json IS PUBLISHED AGAIN, 16 Aug 2026, ON THE OWNER'S STANDING
# AUTHORIZATION: "if in the previous declutter you removed anything based on being
# unpublishable, restore them and publish them (but only if they are really useful and we
# don't have better alternatives!)". Both halves of his test were applied:
#   USEFUL?              161 anchors carrying Christopher Tolkien's own label POSITIONS,
#                        measured off his plates. His backlog names the drawn sheets'
#                        placing as wrong; this is the measurement that fixes it.
#   BETTER ALTERNATIVE?  None. Nothing else in the archive records where HIS labels sit.
# The withdrawal reasoning it replaces is kept in git and in CLAUDE.md; it was correct
# under the licence rule that stood until he lifted it, and it is not deleted, only spent.
# arda_traced.svg above STAYS OUT, and by the same test: it is 9.05 MB and is merely the
# ASSEMBLY of the eight sheets in site/cl/ that are already published. We have the better
# alternative, so his clause excludes it.
# (The withdrawal block was written TWICE. Lifting the first changed nothing, because
#  git was still matching the second -- `git check-ignore -v` named the line in one run.)

# THE TWO UNSTRIPPED CENTRELINE SHEETS, retired from publication 4 August 2026 on the owner's
# ruling -- his words, "yes, the sheets carry linework only." They carried 2,769 and 12,443
# strokes tracing Christopher Tolkien's RED LETTERING; site/cl/bel_nolabel.svg and
# site/cl/gm_nolabel.svg carry the same linework with 84.6% and 66.7% of that removed and are
# now the published sheets. The files stay on disk because the archive still measures its own
# map against his geometry -- the same posture arda_traced.svg has held since 29 July.
# map/lettering_check.py REFUSES if either is tracked again.
cl/bel.svg
cl/gm.svg
.arda_gate_pass
# TRACKED FROM 31 Aug 2026, HIS RULING C716. This file is the SOLE PARENT of the 39
# published idx/ shards, and ignoring it meant a fresh clone could not regenerate the
# index at all -- published output whose source existed in one place only.
!arda_index_merged.json
